<Window x:Class="Mather.Windows.AddTaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sys="clr-namespace:System;assembly=mscorlib" 
        mc:Ignorable="d"
        Title="AddTaskWindow" Height="265" Width="400">
    <Window.Resources>
        <DataTemplate DataType="{x:Type ListBoxItem}">
            <ListBoxItem FontSize="48"/>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="60" />
        </Grid.RowDefinitions>
        <ListBox x:Name="TaskListBox" SelectedIndex="0" />
        <Button Content="OK" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Width="80" Height="24" Click="Button_Click"/>
    </Grid>
</Window>
