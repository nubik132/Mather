<Window x:Class="Mather.NewGroupTeacherWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Mather"
        mc:Ignorable="d"
        Title="Введите название" Height="200" Width="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="64" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <Label Content="Название группы" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <TextBox x:Name="NameBox" Grid.Column="1" Style="{StaticResource Shadow}" VerticalContentAlignment="Center" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="24"/>
        <Button Content="OK" Grid.ColumnSpan="2" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="32 8" Click="Button_Click"/>
    </Grid>
</Window>
